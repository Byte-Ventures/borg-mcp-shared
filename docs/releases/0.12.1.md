This maintenance release corrects the portable protocol v9 conformance suite so
log retry identity is tested against resolved delivery rather than ignored
request fields.

- Changes to class selectors that explicit delivery fields bypass now deduplicate when delivery is unchanged.
- A genuine change to resolved class routing still returns `POST_ID_CONFLICT`.
- Recipient ordering remains set-based, while a different recipient set remains a conflict.
