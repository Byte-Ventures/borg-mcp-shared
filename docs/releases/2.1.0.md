This minor release adds durable-layer guidance to the human-seat roles in the
Software Development, Starter, and Local Model templates.

It also aligns the exported `Decision` type with the server decision record and
requires `ratified_by`, `status`, and `supersedes` when decoding decisions.
