This patch keeps the credential-free protocol mismatch diagnostic aligned with
the protocol version enforced by the package.

- The preflight mismatch message now derives its expected version from `PROTOCOL_VERSION` instead of repeating a separate version literal.
- The existing fail-closed order, non-reflective text, and matching-release guidance remain unchanged.
